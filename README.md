# 耗材管理系统 (Consumable Manager)

基于 Electron + Vue 3 + TypeScript 的耗材管理系统，支持桌面版和Web版两种部署方式。

## 📋 功能特性

- ✅ 耗材信息管理（增删改查）
- ✅ 入库/出库记录管理
- ✅ 二维码扫描功能
- ✅ 数据统计与图表展示
- ✅ 多环境部署（桌面版 + Web版）
- ✅ SQL Server 数据库支持
- ✅ 实时数据库连接测试

## 🚀 快速开始

### 开发环境要求

- Node.js 16.x 或更高版本
- npm 或 yarn
- SQL Server 数据库

### 安装依赖

```bash
npm install
```

### 开发运行

```bash
# 桌面版开发
npm run dev

# Web版开发（前后端同时启动）
npm run dev:full

# 只启动后端服务
npm run dev:backend

# 只启动前端开发服务器
npm run dev:web
```

## 📦 项目打包部署

### 1. EXE桌面版打包

```bash
# 构建并打包Windows桌面版
npm run build:win
```

**输出文件：** `dist-win/consumable-manager 1.0.0.exe`
- 大小：约 374MB
- 特性：独立运行，无需额外安装依赖
- 用户群体：单机使用，本地数据管理

### 2. Web版打包部署

#### 步骤1：构建前端和后端

```bash
# 构建Web前端
npm run build:web

# 构建Web后端
npm run build:backend
```

#### 步骤2：创建部署包

构建完成后，会自动生成 `web-deploy/` 目录，包含：
- `package.json` - 生产环境依赖配置
- `webServer.js` - Web服务器
- `db-web.js` - 数据库模块
- `public/` - 静态文件（HTML/CSS/JS）
- `README.md` - 部署说明

#### 步骤3：服务器部署

```bash
# 进入部署目录
cd web-deploy

# 安装生产依赖
npm install --production

# 启动Web服务
npm start
```

**访问地址：** http://localhost:3001
- 支持多用户并发访问
- 跨平台部署（Windows/Linux/MacOS）
- 轻量化：部署包仅约 36KB

## ⚙️ 数据库配置

系统支持 SQL Server 数据库，首次启动时：

1. **桌面版**：进入"设置"页面配置数据库连接
2. **Web版**：配置保存在服务器端，通过设置页面管理

### 默认配置模板
```json
{
  "server": "your-server-ip",
  "port": 1433,
  "database": "your-database-name",
  "username": "your-username",
  "password": "your-password"
}
```

## 🛠️ 开发脚本说明

| 脚本 | 功能 |
|------|------|
| `npm run dev` | 启动桌面版开发环境 |
| `npm run dev:full` | 同时启动前后端开发环境 |
| `npm run build:win` | 构建Windows桌面版 |
| `npm run build:web` | 构建Web前端 |
| `npm run build:backend` | 构建Web后端 |
| `npm run start:web` | 启动Web版预览 |

## 📁 项目结构

```
consumable-manager/
├── src/
│   ├── main/           # 主进程代码
│   ├── preload/        # 预加载脚本
│   └── renderer/       # 渲染进程（Vue前端）
├── build/              # 构建资源
├── dist-win/           # Windows桌面版输出
├── web-deploy/         # Web版部署包
└── out/                # 构建临时文件
```

## 🔧 技术栈

- **前端**：Vue 3 + TypeScript + Element Plus + ECharts
- **桌面端**：Electron + IPC通信
- **Web端**：Express + Node.js
- **数据库**：SQL Server + mssql驱动
- **构建工具**：Vite + electron-vite + electron-builder

## 🚨 注意事项

1. **数据库连接**：确保SQL Server服务正常运行且网络可达
2. **端口占用**：Web版默认使用3001端口，确保端口未被占用
3. **防火墙**：Web版部署时注意开放对应端口
4. **权限**：确保应用对配置文件和日志目录有读写权限

## 📄 许可证

MIT License
