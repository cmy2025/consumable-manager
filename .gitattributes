# 所有文本文件默认自动转换：仓库存储 LF，Windows 检出转 CRLF
* text=auto

# 单独指定 .gitignore 文件的换行符规则（消除其警告）
.gitignore text eol=crlf

# 脚本文件（.sh/.js/.ts 等）强制使用 LF，避免跨平台执行问题
*.sh text eol=lf
*.js text eol=lf
*.ts text eol=lf
*.vue text eol=lf
*.json text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.md text eol=lf

# 二进制文件（图片/压缩包等）不处理换行符，避免损坏
*.png binary
*.jpg binary
*.zip binary
*.exe binary
*.dll binary